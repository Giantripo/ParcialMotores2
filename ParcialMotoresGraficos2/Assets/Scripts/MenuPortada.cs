using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuPortada : MonoBehaviour
{
    //bool mov =false;
    // float vel;
    //Rigidbody rb;
    //void Start()
    //{
    //    rb = GetComponent<Rigidbody>();
    //    vel = 1;
    //}

    //// Update is called once per frame
    //void Update()
    //{

    //    rb.velocity = new Vector3(1 * vel, rb.velocity.y, 0);

    //    if (this.transform.position.y > 3f && mov == false)
    //    {
    //        vel = vel * -1;
    //    }
     
    //}
}
